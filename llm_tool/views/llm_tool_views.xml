<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Tree View -->
    <record id="view_llm_tool_tree" model="ir.ui.view">
        <field name="name">llm.tool.tree</field>
        <field name="model">llm.tool</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" />
                <field name="implementation" />
                <field name="default" />
                <field name="active" />
                <field name="requires_user_consent" />
                <field name="override_tool_description" optional="hide" />
                <field name="override_tool_schema" optional="hide" />
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_llm_tool_form" model="ir.ui.view">
        <field name="name">llm.tool.form</field>
        <field name="model">llm.tool</field>
        <field name="arch" type="xml">
            <form string="LLM Tool">
                <sheet>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only" />
                        <h1><field
                name="name"
                placeholder="e.g. retrieve_records"
              /></h1>
                    </div>
                    <group>
                        <group>
                            <field name="implementation" />
                            <field name="default" />
                            <field name="active" />
                            <field name="requires_user_consent" />
                            <field name="override_tool_description" />
                            <field name="override_tool_schema" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field
                name="description"
                placeholder="Describe what this tool does and when to use it"
              />
                        </page>
                        <page string="Schema">
                            <field
                name="schema"
                widget="text"
                placeholder="{...}"
                readonly="1"
              />
                            <separator
                string="Override Schema"
                attrs="{'invisible': [('override_tool_schema', '=', False)]}"
              />
                            <field
                name="overriden_schema"
                widget="text"
                placeholder="Enter custom JSON schema here"
                attrs="{'invisible': [('override_tool_schema', '=', False)], 'required': [('override_tool_schema', '=', True)]}"
              />
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" />
                    <field name="message_ids" />
                </div>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_llm_tool_search" model="ir.ui.view">
        <field name="name">llm.tool.search</field>
        <field name="model">llm.tool</field>
        <field name="arch" type="xml">
            <search string="Search LLM Tools">
                <field name="name" />
                <field name="implementation" />
                <filter
          string="Default Tools"
          name="default"
          domain="[('default', '=', True)]"
        />
                <filter
          string="Active"
          name="active"
          domain="[('active', '=', True)]"
        />
                <filter
          string="Override Description"
          name="override_desc"
          domain="[('override_tool_description', '=', True)]"
        />
                <filter
          string="Override Schema"
          name="override_schema"
          domain="[('override_tool_schema', '=', True)]"
        />
                <group expand="0" string="Group By">
                    <filter
            string="Implementation"
            name="group_by_implementation"
            context="{'group_by': 'implementation'}"
          />
                    <filter
            string="Override Schema"
            name="group_by_override_schema"
            context="{'group_by': 'override_tool_schema'}"
          />
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_llm_tool" model="ir.actions.act_window">
        <field name="name">LLM Tools</field>
        <field name="res_model">llm.tool</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new LLM Tool
            </p>
            <p>
                Define tools that LLM models can use to interact with Odoo.
            </p>
        </field>
    </record>
</odoo>
